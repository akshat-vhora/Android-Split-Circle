class CacheDurations {
  static const settlements = Duration(seconds: 15);
  static const dashboardBalances = Duration(seconds: 30);
  static const expenses = Duration(seconds: 30);
  static const expenseDetail = Duration(seconds: 60);
  static const friends = Duration(minutes: 5);
  static const profile = Duration(minutes: 10);
  static const notifications = Duration(seconds: 30);
}
